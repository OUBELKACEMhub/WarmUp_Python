[project]
name = "warmup-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "OUBELKACEM Ahmed", email = "oubelkacemahmed78@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.5",
]

[project.scripts]
warmup-python = "warmup_python:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
